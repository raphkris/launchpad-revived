import SwiftUI

/// Placeholder SwiftUI content hosted during scaffolding (PRJ-02).
struct EmptyRootView: View {
    var body: some View {
        Color.clear
            .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}
